import { IonButton } from '@ionic/react';
import React from 'react';

const Logout = (props:any) => {
    

    return (
        <>
            <IonButton>
                Logout
            </IonButton>
        </>
    );
}

export default Logout;
